/**
 * Generic untyped module definition for .scss files so we can
 * import them in typescript, required for CSS modules
 */
declare module '*.module.css';
/**
 * Generic untyped module definition for .svg files so we can
 * import them in typescript, required @svgo/webpack loader
 */
declare module '*.svg';
